# Brew

```sh
# Update brew and formulae
brew update
# Upgrade packages (casks)
brew upgrade
# Install a package
brew install gleam
# Remove a package
brew uninstall gleam
# Pin a package
brew pin gleam
# Remove orphaned packages
brew autoremove
# Clean up
brew cleanup
# See top-level packages I installed
brew leaves -r
```
