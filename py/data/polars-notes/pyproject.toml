[project]
name = 'polars-notes'
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.12.0",
    "pydash>=8.0.4",
]

[tool.uv]
dev-dependencies = ["ruff", "pylint"]

[tool.ruff.lint]
extend-select = ["I"]

[tool.pyright]
typeCheckingMode = "basic"
