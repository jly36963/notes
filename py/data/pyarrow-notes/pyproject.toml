[project]
name = 'pyarrow-notes'
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["numpy>=2.1.3", "pandas>=2.2.3", "pyarrow>=18.0.0"]

[tool.uv]
dev-dependencies = ["ruff", "pylint"]

[tool.ruff.lint]
extend-select = ["I"]

[tool.pyright]
typeCheckingMode = "basic"
